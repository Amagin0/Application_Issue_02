<%= form_with(model:[book, book_comment], remote: true) do |f| %>
    <div class="form-group">
      <%= f.text_area :comment, rows:"4", class:'col-md-12' %>
    </div>
    <div class="form-group">
      <%= f.submit "送信" %>
    </div>
<% end %>